import React from 'react';
import { Menu } from '@blueprintjs/core';

const UserMenu = () => (
  <Menu>
    <Menu.Item icon="new-text-box" onClick={() => {}} text="New text box" />
    <Menu.Item icon="new-object" onClick={() => {}} text="New object" />
    <Menu.Item icon="new-link" onClick={() => {}} text="New link" />
  </Menu>
);

export default UserMenu;
